# Parser Fixture Pack

These fixtures define deterministic expectations for future parser heuristic tests.

They are design assets only:

- no parser logic is implemented here
- no ranking code is implied here
- the manifest records the expected candidate order, candidate cap, and false-positive exclusions

Use `heuristic_ranking_cases.json` as the source of truth for expected outputs.
